name: Deploy (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment disabled in GitHub Actions
        run: |
          echo "GitHub deploy workflow is intentionally disabled."
          echo "Use CircleCI pipeline for validation and deployment."
